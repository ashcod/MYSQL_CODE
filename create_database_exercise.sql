-- Create the “Sales” database. To do this, you’ll have to use one of the two possibilities shown in the lecture, and then execute the code!

-- Creating a database called Sales
-- IF NOT EXISTS will check if the database is already exists or not

CREATE DATABASE IF NOT EXISTS Sales;

-- Creating a Sales_new database [Alternative to create database]
CREATE SCHEMA IF NOT EXISTS Sales;
